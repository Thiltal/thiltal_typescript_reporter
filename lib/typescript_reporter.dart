library typescript_reporter;

class Typescript {
  const Typescript();
}

class TypescriptOptional {
  const TypescriptOptional();
}

class TypescriptSkip {
  const TypescriptSkip();
}

class TypescriptReplaceType {
  final String newType;
  const TypescriptReplaceType(this.newType);
}
